import { StrategyDiagnosticsView } from "@/components/diagnostics/StrategyDiagnosticsView";

export const dynamic = "force-dynamic";

export default function DiagnosticsPage() {
  return <StrategyDiagnosticsView />;
}
